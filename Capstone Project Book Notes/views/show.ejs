<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= book.title %> - Review | The Book Chronicles</title>
  <link rel="stylesheet" href="/styles/main.css" />
</head>
<body>
  <nav class="navbar">
    <div class="navbar-container">
      <a href="/" class="logo" style="text-decoration: none;">📚 The Book Chronicles</a>
      <a href="/new" class="btn btn-primary btn-sm">+ Write a Review</a>
    </div>
  </nav>

  <header class="hero-header">
    <div class="hero-content">
      <h1><%= book.title %></h1>
      <p class="hero-subtitle">by <strong><%= book.author %></strong></p>
    </div>
  </header>

  <div class="container">
    <div class="book-detail">
      <div class="book-cover large">
        <% if (book.coverUrl) { %>
          <img src="<%= book.coverUrl %>" alt="<%= book.title %> cover" />
        <% } %>
      </div>

      <div style="background: white; padding: 32px; border-radius: 12px; box-shadow: 0 4px 16px rgba(0,0,0,0.12); margin-bottom: 32px;">
        <% if (book.rating) { %>
          <div class="article-meta">
            <div class="meta-item rating-badge">
              <% for (let i = 1; i <= 5; i++) { %>
                <span class="star <%= i <= book.rating ? 'filled' : '' %>">★</span>
              <% } %>
              <span class="rating-text"><%= book.rating %>/5</span>
            </div>
          </div>
        <% } %>

        <% if (book.date_read) { %>
          <p style="color: #7f8c8d; margin-bottom: 16px; font-family: 'Segoe UI', Roboto, sans-serif;">
            📅 Read on <strong><%= new Date(book.date_read).toLocaleDateString('en-US', {
              year: 'numeric',
              month: 'long',
              day: 'numeric'
            }) %></strong>
          </p>
        <% } %>
      </div>

      <section class="full-review">
        <h2>Review & Notes</h2>

        <% if (book.notes) { %>
          <p><%= book.notes %></p>
        <% } else { %>
          <p><em>No review written for this book yet.</em></p>
        <% } %>
      </section>

      <div class="article-footer" style="margin-top: 32px;">
        <a href="/" class="read-more" style="margin-left: -4px;">← Back to Library</a>
        <div class="article-actions">
          <a href="/edit/<%= book.id %>" class="btn-icon btn-edit-icon" title="Edit review">
            ✏️
          </a>
          <form
            action="/books/<%= book.id %>/delete"
            method="POST"
            style="display: inline;"
            onsubmit="return confirm('Are you sure you want to delete this book?')"
          >
            <button class="btn-icon btn-delete-icon" title="Delete review">
              🗑️
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>
  <footer class="footer" style="margin-top: 60px;">
    <div class="footer-content">
      <p>The Book Chronicles © 2024 | Happy reading 📚</p>
    </div>
  </footer>
</body>
</html>
