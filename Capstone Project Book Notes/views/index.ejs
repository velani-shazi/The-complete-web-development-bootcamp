<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Book Notes</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>📚 My Book Notes</h1>
            <p class="subtitle">A personal collection of books I've read and loved</p>
            <div class="stats">
                <span class="book-count">Total Books: <%= totalBooks %></span>
            </div>
        </header>

        <div class="action-bar">
            <a href="/new" class="btn btn-primary">+ Add New Book</a>
            
            <div class="sort-controls">
                <span class="sort-label">Sort by:</span>
                <a href="/?sort=rating" class="btn-sort <%= currentSort === 'rating' ? 'active' : '' %>">
                    ⭐ Rating
                </a>
                <a href="/?sort=date" class="btn-sort <%= currentSort === 'date' ? 'active' : '' %>">
                    📅 Date Read
                </a>
                <a href="/?sort=title" class="btn-sort <%= currentSort === 'title' ? 'active' : '' %>">
                    🔤 Title
                </a>
            </div>
        </div>

        <% if (books.length === 0) { %>
            <div class="empty-state">
                <h2>No books yet!</h2>
                <p>Start adding books you've read to build your collection.</p>
                <a href="/new" class="btn btn-primary">Add Your First Book</a>
            </div>
        <% } else { %>
            <div class="books-grid">
                <% books.forEach(book => { %>
                    <div class="book-card">
                        <div class="book-cover">
                            <% if (book.isbn) { %>
                                <img 
                                    src="<%= book.coverUrl %>" 
                                    alt="<%= book.title %> cover"
                                    onerror="this.src='https://via.placeholder.com/200x300?text=No+Cover'"
                                >
                            <% } else { %>
                                <div class="no-cover">
                                    <span>📖</span>
                                    <p>No Cover</p>
                                </div>
                            <% } %>
                        </div>

                        <div class="book-info">
                            <h3 class="book-title"><%= book.title %></h3>
                            <p class="book-author">by <%= book.author %></p>

                            <% if (book.rating) { %>
                                <div class="rating">
                                    <% for(let i = 1; i <= 5; i++) { %>
                                        <% if (i <= book.rating) { %>
                                            <span class="star filled">★</span>
                                        <% } else { %>
                                            <span class="star">☆</span>
                                        <% } %>
                                    <% } %>
                                    <span class="rating-number">(<%= book.rating %>/5)</span>
                                </div>
                            <% } %>

                            <% if (book.date_read) { %>
                                <p class="date-read">
                                    📅 Read: <%= new Date(book.date_read).toLocaleDateString('en-US', { 
                                        year: 'numeric', 
                                        month: 'long', 
                                        day: 'numeric' 
                                    }) %>
                                </p>
                            <% } %>

                            <% if (book.notes) { %>
                                <div class="notes-preview">
                                    <p>
                                        <%= book.notes.length > 150 
                                            ? book.notes.substring(0, 150) + '...' 
                                            : book.notes %>
                                    </p>
                                </div>
                            <% } %>

                            <div class="book-actions">
                                <a href="/edit/<%= book.id %>" class="btn btn-edit">Edit</a>
                                <form action="/books/<%= book.id %>/delete" method="POST" style="display: inline;">
                                    <button 
                                        type="submit" 
                                        class="btn btn-delete"
                                        onclick="return confirm('Are you sure you want to delete this book?')"
                                    >
                                        Delete
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>

        <footer>
            <p>Built with ❤️ for book lovers | Powered by <a href="https://openlibrary.org" target="_blank">Open Library API</a></p>
        </footer>
    </div>
</body>
</html>